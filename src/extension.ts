import * as vscode from 'vscode';

/**
 * This method is called when your extension is activated
 * Your extension is activated the very first time the command is executed
 */
export function activate(_context: vscode.ExtensionContext) {
  console.log('DocuFold extension is now active!');

  // TODO: Initialize services and providers
  // TODO: Register commands
  // TODO: Register folding range provider
  // TODO: Setup status bar integration
}

/**
 * This method is called when your extension is deactivated
 */
export function deactivate() {
  console.log('DocuFold extension is now deactivated!');
}
